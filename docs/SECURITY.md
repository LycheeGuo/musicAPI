# 安全与版权边界

本仓库的聚合引擎用于连接你有权使用的音乐播放 API。

默认不内置：
- 绕过会员/VIP/付费墙的接口
- 绕过登录、区域限制或访问控制的逻辑
- Cookie/Token/账号凭据
- 自动执行第三方混淆脚本

`upstreams/` workflow 只观察公开仓库文件 SHA 是否变化，不复制或执行其代码。

如果 Provider 需要私密凭据，不要放在 GitHub 配置或 SPlayer 插件里。应把凭据放在你控制的服务端，由插件访问服务端。
